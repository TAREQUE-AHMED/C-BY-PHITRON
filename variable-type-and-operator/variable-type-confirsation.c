#include<stdio.h>
int main(){
    // double number = 10.24;
    // printf("Number = %d", (int)number);
    int number1=21, number2 = 2;
    double division = (double)number1 / (double)number2;
    printf("%d / %d=%lf ", number1, number2, division);
    return 0;
}